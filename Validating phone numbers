import re

n = int(raw_input())
for i in range(n):
    l = raw_input()
    pattern = re.compile("[7-9]\d{9}")
    if len(l) == 10 and pattern.match(l):
        print "YES"
    else:
        print "NO"
